pipeline {
    agent any 
        tools {
            jdk 'openjdk17'
            maven 'Maven-3.6.3'
        }
    stages {
      stage (version) {
        steps {
            sh ''' echo "below version is ${image}" '''
        }
      }
    }
}
